extends layout

block content
  main
    aside#photos
      img(src="/images/chili.jpg" alt="White Chicken Chili" width="180")
    
    //- Render the custom blogtitle element
    h2: blogtitle

    section#blogposts
      ul
        //- Loop through Vue posts array
        li#posts(v-for="post in posts")
          span.profile
            img(v-bind:src="post.profilepic" v-on:click="authorinfo(posts, post)")
          span.author {{ post.name }}
          |  — 
          span.date {{ post.date }}
          span.reply {{ post.reply }}
          p {{ post.message }}
        
        //- Vue info box popup triggered by thisBox state
        div#info_box(v-for="post in posts" v-if="post.thisBox")
          p.closeBtn.closeBtnP(v-on:click="$emit('close', post.thisBox = false)") &#10006;
          div#info_title {{ post.name }}
          div#info_foodie
            span Foodie Level: 
            | {{ post.foodielevel }}
          div#info_bio
            span Bio:
            br
            | {{ post.bio }}
          button.closeBtn.closeBtnB(v-on:click="$emit('close', post.thisBox = false)") Close
          p#close_info